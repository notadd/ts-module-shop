type Mutation{
    createBrand(name:String!,logo:BrandLogo!):ShopCommonData
    updateBrand(id:Int!,name:String!):ShopCommonData
    deleteBrand(id:Int!):ShopCommonData
}

input BrandLogo{
    bucketName：String!
    rawName:String!
    base64:String!
}

type Query{
    brands:BrandsData
}

type BrandsData{
    code:Int
    message:String
    brands:[Brand]
}

type Brand{
    id:Int
    name:String
}