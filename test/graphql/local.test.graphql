mutation config{
  public:bucket(
    isPublic:true,
    name:"public"
  ){
    code
    message
  }
  private:bucket(
    isPublic:false,
    name:"private",
    tokenExpire:20,
    tokenSecretKey:"adasdaesdawddsdassd"
  ){
    code
    message
  }
  imageFormat(format:raw){
    code
    message
  }
  enableImageWatermark(enable:false){
		code
    message
  }
  audioFormat(format:raw){
    code
    message
  }
   videoFormat(format:raw,resolution:raw){
		code
    message
  }
}