type Mutation{
    #config format of uploaded image
    imageFormat(format:ImageFormat!):ConfigData
}

#the enum of image format
enum ImageFormat {
  #original format 
  raw
  #webp 
  webp_damage
  #webp and lossless
  webp_undamage
}